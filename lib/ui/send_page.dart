import 'package:flutter/material.dart';

class SendingPage extends StatelessWidget{
  const SendingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Sending Page');
  }
}
